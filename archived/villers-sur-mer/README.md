# Archived Villers-sur-Mer Outputs

Story outputs from Villers-sur-Mer world sessions. All use the old Writer architecture (persistent Opus agent, story.md monolith).

## Sessions

### `auto-20260212/`
Early auto-seeded session. Minimal output.

### `auto-20260214/`
Auto-seeded session.

### `auto-20260215/`
Auto-seeded session with character agent files.

### `20260225-ce-que-la-mer-rend/`
"Ce que la mer rend" seed. Includes character agent files.

### `l-echange/`
"L'Échange" session. Story and state only.

## Note

All sessions here use the old Writer architecture (persistent Opus agent, story.md monolith). The current system uses the Stitcher architecture (fresh-per-chapter agent reading character output files from disk, per-turn chapter.md files). Active outputs live in `output/`.
